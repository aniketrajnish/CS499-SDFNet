using System;
using UnityEngine;
public class Helpers : MonoBehaviour
{
    /*public static Vector3 GetDimensionVectors(int i)
    {
        int len = Enum.GetNames(typeof(RaymarchRenderer.Shape)).Length;

        Vector3[] dimensions = new Vector3[len];

        //cylinder
        dimensions[0] = new Vector3(CylinderDimensions.r, CylinderDimensions.h, 0);

        //capped cone
        dimensions[1] = new Vector3(CappedConeDimensions.r1, CappedConeDimensions.r2, CappedConeDimensions.h);        

        return dimensions[i];
    }*/
}